/**
 * Match orchestration: MatchActor (single-threaded per-match state machine), MatchState, TurnManager.
 *
 * <p>Placeholder for M0 scaffolding; real classes land in later milestones
 * per PLAN.md section 1 (Project Structure).
 */
package com.brutaltank.match;
